.row
  .small-12.columns
    =image_tag blog['blog.image'].views['lead'].url
.row
  .small-12.columns
    %h6.subheader= blog['blog.date'].value.strftime("%B %d, %Y")
.row
  .small-12.columns
    %h2= link_to blog['blog.title'].value, link_resolver().link_to(blog)
.row
  .small-12.columns
    %p= truncate(blog['blog.body'].as_html_safe(link_resolver), length: chars, separator: ' ') { link_to ' Continue Reading', '#'}